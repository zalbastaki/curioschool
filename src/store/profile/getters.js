const getters = {
    role: state => {
        return state.role;
    },

    profile: state => {
        return state.profile;
    },

    unsubscribe: state => {
        return state.unsubscribe;
    },
};

export default getters;
